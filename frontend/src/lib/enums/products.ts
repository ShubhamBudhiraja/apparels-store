export enum CART_PRODUCT_OPERATION {
    INCREASE = 'increase',
    DECREASE = 'decrease',
}

export enum CURRENCY {
    INR = 'inr',
}

export enum CHECKOUT_STEPS {
    CART,
    SHIPPING,
    PAYMENT,
}

export enum VARIANT_ID {
    DEFAULT = 'default',
}
